export const environment = {
  production: true,
  // Cuando subas el backend a la nube (AWS, DigitalOcean, etc.),
  // pondrás aquí la URL real (ej: 'https://api.misisistema.com').
  // Por ahora, déjalo igual o pon una cadena vacía para que no falle el build.
  apiUrl: 'http://localhost:3000/api/v1'
};